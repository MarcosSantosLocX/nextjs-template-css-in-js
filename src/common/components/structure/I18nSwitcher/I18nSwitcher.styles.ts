import { styled } from '@/styles/stitches.config';

export const I18nSwitcherContainer = styled('div', {
  width: '100%',
  display: 'block',
  textAlign: 'center',
});

export const Paragraph = styled('p', {
  fontSize: '$8',
});

export const UlList = styled('ul', {
  'li a': {
    fontSize: '$8',
  },
});
